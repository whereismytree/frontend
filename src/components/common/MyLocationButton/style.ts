import styled from 'styled-components';

export const MyLocationButton = styled.img`
  cursor: pointer;
  width: 3.8rem;
  height: 3.7rem;
  box-shadow: 0 7px 5px 5px rgba(0, 0, 0, 0.07);
  border-radius: 0.5rem;
`;
